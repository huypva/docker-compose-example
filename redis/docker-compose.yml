version: "2"
services:
  redis:
    image: "redis:alpine"
    container_name: 'redis'
    ports:
      - 6379:6379
    networks:
      - infra-network

networks:
  infra-network:
    name: infra-network
    external: true